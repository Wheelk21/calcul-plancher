function textePlanche(n){return n+" planche"+(n>1?"s":"");}
function creerCheckboxes(){const container=document.getElementById("planches");const actives=chargerPlanchesActives();container.innerHTML=CONFIG.planches.map(largeur=>`<label class="check"><input type="checkbox" class="plank-check" value="${largeur}" ${actives.includes(largeur)?"checked":""}><span>${largeur} mm</span></label>`).join("");}
function planchesActives(){return Array.from(document.querySelectorAll(".plank-check")).filter(check=>check.checked).map(check=>parseInt(check.value,10)).sort((a,b)=>b-a);}
function ordrePose(solution,actives){const ordre=[];for(const largeur of actives){for(let i=0;i<(solution.counts[largeur]||0);i++){ordre.push(largeur);}}return ordre;}
function renderRows(solution){return CONFIG.planches.map(largeur=>`<div class="row"><span>${largeur} mm</span><span class="value">${textePlanche(solution.counts[largeur]||0)}</span></div>`).join("");}
function renderSolution(solution,index,actives){const titre=index===0?"Solution idéale":"Alternative "+index;const badge=index===0?"Meilleure":"Conforme";const badgeClass=index===0?"badge":"badge alt";const bestClass=index===0?"solution best":"solution";const chips=ordrePose(solution,actives).map(x=>`<span class="chip">${x}</span>`).join("");return `<article class="${bestClass}"><h2><span>${titre}</span><span class="${badgeClass}">${badge}</span></h2><div class="rows">${renderRows(solution)}<div class="sep"></div><div class="row"><span>Total</span><span class="value">${textePlanche(solution.total)}</span></div><div class="row"><span>Largeur obtenue</span><span class="value">${solution.obtenu} mm</span></div><div class="row"><span>Écart réel</span><span class="value good">${solution.ecart} mm</span></div><div class="row"><span>Écart maximum</span><span class="value">${CONFIG.ecartMaxFixe} mm</span></div><div class="row"><span>Dépassement</span><span class="value good">0 mm</span></div></div><div class="chips">${chips}</div></article>`;}
